// ShopDashboardApp.swift
// App entry point. Minimal by design — all setup happens in ContentView.
// @main tells Swift this is where the app starts.

import SwiftUI

@main
struct ShopDashboardApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
